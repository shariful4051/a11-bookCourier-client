import React from 'react';
import useAuth from '../Hooks/useAuth';
import useStatus from '../Hooks/useStatus';

const AdminRout = () => {
    const {user,loding}=useAuth()
    const {status,statusLoding} = useStatus()
    if(lo)
    return (
        <div>
            
        </div>
    );
};

export default AdminRout;